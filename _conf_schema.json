{
    "SERVERS": {
        "description": "【多服务器·核心】MC服务器配置列表",
        "type": "template_list",
        "hint": "点击「添加」新增一台MC服务器。每台服务器独立配置监听端口、回传通道、机器人名等。server_name 必须与 MC 端 config.yml 的 server_name 一致。",
        "templates": {
            "server_template": {
                "name": "MC服务器",
                "items": {
                    "name": {
                        "description": "服务器名称",
                        "hint": "唯一标识，必须与MC端 config.yml 的 server_name 一致。玩家标签格式: mcs[名称].玩家名",
                        "type": "string",
                        "default": "survival"
                    },
                    "host": {
                        "description": "MC服务器IP",
                        "hint": "本插件向该服回传 tellraw 时连接的IP。同机部署填 127.0.0.1",
                        "type": "string",
                        "default": "127.0.0.1"
                    },
                    "listen_port": {
                        "description": "AstrBot监听端口",
                        "hint": "本插件为该服务器独占的HTTP监听端口，每台服必须不同（如6188、6189...）",
                        "type": "int",
                        "default": 6188
                    },
                    "bridge_token": {
                        "description": "鉴权Token",
                        "hint": "与MC端 bridge_token 填一致，为空则不鉴权。生产环境务必设置",
                        "type": "string",
                        "default": ""
                    },
                    "send_channel": {
                        "description": "回传通道",
                        "hint": "bridge=走MC端插件HTTP（需装McAstrbotBridge.jar），rcon=走原生RCON（需MC端开启rcon）",
                        "type": "string",
                        "default": "bridge",
                        "options": ["bridge", "rcon"]
                    },
                    "mc_bridge_port": {
                        "description": "MC端HTTP端口",
                        "hint": "send_channel=bridge 时用，对应MC端 bridge_listen_port（默认25580）",
                        "type": "int",
                        "default": 25580
                    },
                    "mc_rcon_port": {
                        "description": "RCON端口",
                        "hint": "send_channel=rcon 时用，对应MC端 server.properties 的 rcon.port（默认25575）",
                        "type": "int",
                        "default": 25575
                    },
                    "mc_rcon_password": {
                        "description": "RCON密码",
                        "hint": "send_channel=rcon 时用，对应MC端 rcon.password",
                        "type": "string",
                        "default": ""
                    },
                    "bot_name": {
                        "description": "机器人显示名",
                        "hint": "本服机器人的名字。tellraw 模板里 {BOT_NAME} 会替换为此值",
                        "type": "string",
                        "default": "Kei"
                    },
                    "tellraw_template": {
                        "description": "tellraw消息模板",
                        "hint": "支持 {BOT_NAME} 和 {message} 占位符，支持 § 颜色代码",
                        "type": "string",
                        "default": "§7<{BOT_NAME}> {message}"
                    },
                    "trigger_keywords": {
                        "description": "触发关键词",
                        "hint": "玩家消息含任一关键词即触发LLM回复。多个用英文逗号分隔，如: Kei,机器人,小樱",
                        "type": "string",
                        "default": "Kei,机器人"
                    },
                    "enable_at_trigger": {
                        "description": "启用@触发",
                        "hint": "玩家消息以 @{bot_name} 或 {bot_name} 开头时触发回复",
                        "type": "bool",
                        "default": true
                    },
                    "online_mode": {
                        "description": "正版验证",
                        "hint": "建议不手动填。MC端启动握手会自动用 Bukkit.getOnlineMode() 覆盖此值",
                        "type": "bool",
                        "default": false
                    },
                    "player_whitelist": {
                        "description": "玩家白名单",
                        "hint": "非空时只有白名单玩家的消息会被记录。留空=不限制。多个用英文逗号分隔",
                        "type": "string",
                        "default": ""
                    },
                    "player_blacklist": {
                        "description": "玩家黑名单",
                        "hint": "命中直接忽略其消息。多个用英文逗号分隔",
                        "type": "string",
                        "default": ""
                    },
                    "message_filter": {
                        "description": "消息过滤词",
                        "hint": "消息含任一关键词直接忽略。多个用英文逗号分隔",
                        "type": "string",
                        "default": ""
                    }
                }
            }
        }
    },
    "ASTRBOT_LISTEN_HOST": {"description": "【连接】AstrBot端HTTP接收服务绑定的主机IP。默认0.0.0.0全网卡监听；同机部署可改为127.0.0.1增强安全", "type": "string", "default": "0.0.0.0"},
    "SUPER_ADMIN_IDS": {"description": "【权限】超级管理员ID列表(逗号分隔QQ号/AstrBot平台用户ID)，拥有全部MC指令执行权限(含风险游戏操作类)", "type": "text", "default": ""},
    "ADMIN_CAN_QUERY_ONLY": {"description": "【权限】PermissionType.ADMIN群管是否可执行查询类+插件运维类(禁用则仅SUPER_ADMIN能做任何MC指令)", "type": "bool", "default": true},
    "ENABLE_NATURAL_LANGUAGE_TOOL": {"description": "【权限】是否注册LLM自然语言工具(管理员用自然语言意图触发MC命令)；关闭后仅/mc命令可用", "type": "bool", "default": true},
    "CMD_CONFIRMATION_REQUIRED": {"description": "【权限】玩家管理类/游戏操作类执行前，是否要求AstrBot管理员发送二次确认消息(确认/是/y/好)，确认后才执行", "type": "bool", "default": false},
    "CMD_EXECUTE_TIMEOUT": {"description": "【权限】二次确认超时时间(秒)，超时自动取消", "type": "int", "default": 120},
    "ENABLE_CROSS_SERVER_PUNISH": {"description": "【处罚联动】启用正版服之间处罚跨服联动：踢/禁言/白名单时在所有online_mode=true的服务器同步执行", "type": "bool", "default": true},
    "ENABLE_LLM_REPLY": {"description": "【回复·总开关】是否启用LLM回复被触发的玩家消息", "type": "bool", "default": true},
    "LLM_REPLY_COOLDOWN": {"description": "【回复】LLM回复同服同一玩家的冷却时间(秒)，0=不限", "type": "int", "default": 3},
    "MAX_HISTORY_PER_SERVER": {"description": "【会话】每台服务器虚拟会话保留的最大消息条数", "type": "int", "default": 300},
    "LLM_CONTEXT_COUNT": {"description": "【会话】调用LLM时携带的近期消息条数(本服务器)", "type": "int", "default": 20},
    "ENABLE_CROSS_SERVER_CONTEXT": {"description": "【正版同步】开启正版验证(online_mode=true)且同一玩家在其他正版服有聊天时，LLM回复时补充读取那些服务器的近期消息作上下文", "type": "bool", "default": true},
    "ENABLE_SYNC_TO_MEMORY_COMPANION": {"description": "【同步】是否将MC玩家聊天/触发回复事件同步到 memory_companion 伴侣插件", "type": "bool", "default": true},
    "MEMORY_COMPANION_SYNC_INTERVAL": {"description": "【同步】每多少条消息批量同步一次到memory_companion(防刷)，1=每条即同步", "type": "int", "default": 5},
    "ENABLE_SYNC_TO_IMPRESSION": {"description": "【同步】是否为MC玩家生成/更新交互印象(impression插件)；触发LLM回复或达到交互次数时更新", "type": "bool", "default": true},
    "IMPRESSION_TRIGGER_COUNT": {"description": "【同步】MC玩家在同一服务器累计交互多少次，触发一次印象更新(LLM生成)", "type": "int", "default": 8}
}
